// Use this script on overpass to regenerate the munich polygon overlays

// https://overpass-turbo.eu/

// https://overpass-turbo.eu/s/1nWz


/*
This has been generated by the overpass-turbo wizard.
The original search was:
“boundary=administrative in munich”
*/
[out:json][timeout:25];
// fetch area “munich” to search in
{{geocodeArea:munich}}->.searchArea;
// gather results
(
  // query part for: “boundary=administrative”
  way["boundary"="administrative"][admin_level=9](area.searchArea);
  way["boundary"="administrative"][admin_level=10](area.searchArea);
  relation["boundary"="administrative"][admin_level=9](area.searchArea);
  relation["boundary"="administrative"][admin_level=10](area.searchArea);
);
// print results
out body;
>;
out skel qt;